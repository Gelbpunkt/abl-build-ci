
name: Build unsigned ABL image

on:
  push:
  workflow_dispatch:

jobs:
  build:
    name: Build and upload unsigned ABL
    runs-on: ubuntu-22.04

    strategy:
      fail-fast: false
      matrix:
        include:
          - target: waipio
            patch_dir: patch-pineapple
            abl_dir: abl-pineapple
          - target: kailua
            patch_dir: patch-pineapple
            abl_dir: abl-pineapple
          - target: pineapple
            patch_dir: patch-pineapple
            abl_dir: abl-pineapple
          - target: sun
            patch_dir: patch-pineapple
            abl_dir: abl-pineapple

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Ensure build script is executable
        run: chmod +x ./build.sh

      - name: Run build.sh for matrix target
        run: |
          set -eux
          sudo apt-get update
          sudo apt-get install -y clang gcc-aarch64-linux-gnu uuid-dev
          git submodule update --init --depth=1
          ./build.sh "${{ matrix.abl_dir }}" "${{ matrix.patch_dir }}" "${{ matrix.target }}"

      - name: Upload unsigned ABL artifact for target
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.target }}-unsigned-abl.elf
          path: unsigned_abl.elf

